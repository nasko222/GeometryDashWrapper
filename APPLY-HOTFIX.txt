OVERKILLTEST1 STARTUP HOTFIX 1

Copy the CONTENTS of this folder into your existing
GeometryDashArmWrapper overkilltest1 source folder and replace files.

Then run:
  BUILD_WINDOWS.cmd

The existing Unicorn object cache is preserved. Only the wrapper should need
to rebuild/relink.

Fixes:
- Explicit uint64_t bounds for Unicorn uc_ctl_remove_cache on 32-bit Windows.
- Optional diagnostic patches no longer abort startup if one is unavailable.
