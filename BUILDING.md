# Building Geometry Dash Wrapper 0.9.6-gdpstweaks15 on Windows

Use the normal project build flow (`BUILD_ALL.cmd`). Rebuild the x86 and ARMv7 executables; tweaks14 changes both backends. The shared audio implementation is unchanged.
