# Building Geometry Dash Wrapper 0.9.6-gdpstweaks16 on Windows

Use the normal project build flow (`BUILD_ALL.cmd`). The functional tweak16 change is confined to the ARMv7 backend; version/build metadata changes also update the launcher and diagnostic strings. The x86 backend and shared audio implementation are byte-for-byte unchanged from tweaks15.
