# EnduranceTest7

Focused July 30 follow-up:

1. preserves the confirmed working Practice Mode Z/X controls unchanged;
2. removes every legacy MCI volume reassert/maintenance experiment;
3. stops carrying the stored Android volume into newly opened MCI aliases, so
   the first level play uses the same device-default loudness as later restarts;
4. rebuilds 2.2 BPM/time markers exactly once for each editor session;
5. reproduces the selected beta companion's exact object-opacity policy
   (0/70/255) instead of forcing every activated object fully opaque.

The right-side black region, BPM guides and legacy attempt-volume equality are
runtime targets, not pre-test claims. No x86 pacing or Z/X code is changed.
