NETWORKTEST4 SOURCE UPDATE
==========================

Baseline:
  GeometryDashWrapper 0.9.4-arm-v22beta-networktest3-wall-watchdog full source

Recommended:
  Extract the NetworkTest4 full-source archive as a separate branch.

Patch method:
  From the root of a clean NetworkTest3 source tree:

    git apply GeometryDashWrapper-0.9.4-arm-v22beta-networktest4-dyn14-wake-trace.diff

The patch was apply-tested against the supplied NetworkTest3 source and the
result was byte-for-byte identical to the packaged NetworkTest4 tree.

Build on Windows:

    BUILD_V22BETA_X64.cmd game-v22beta-selected.apk

Expected output:
  dist-arm-wrapper-v22beta-networktest4-dyn14-wake-trace\
