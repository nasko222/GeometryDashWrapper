Geometry Dash Wrapper 0.9.6-publictest23 focused verification

Runtime evidence from PublicTest22
----------------------------------
Forlorn 0.101:
  - PT22 allocation reuse is active.
  - Heap cursor reaches exactly 0x38000000, the end of the 128 MiB arena.
  - Final request is 240 bytes with only fragmented blocks <=128 bytes remaining.

Forlorn 1.9c:
  - Fault address is 0x14c.
  - Fault dump shows r4=0 while executing PlayLayer -addSpriteSheets.
  - Static ARMv7 disassembly shows addSpriteSheets stores self in r4 and later performs the failing [r4 + 0x14c] ivar read.
  - The real level-loading thread target is a PlayLayer instance.

PublicTest23 changes
--------------------
  - iOS guest heap expanded from 128 MiB to 256 MiB; no other guest region moved.
  - Allocation failure diagnostics include live block/byte and reuse counts.
  - The current real PlayLayer is tracked at allocation and NSThread initialLoading dispatch.
  - addSpriteSheets boundaries log PC/LR/r0/r4 and tracked self.
  - A zero r0 at addSpriteSheets entry is restored from the tracked PlayLayer.
  - A zero r4 while in/returning to addSpriteSheets is restored from the tracked PlayLayer.
  - Normal guest instance dispatch explicitly reinstates r0=receiver before entering the guest IMP.
  - No global nil Objective-C receiver substitution is added.
  - Android x86/ARM runtime logic is unchanged apart from displayed PublicTest23 version metadata.

Windows runtime execution is not available in this environment; both supplied versions should be re-tested and the PT23 ios-armv7 logs retained.

Local static checks
-------------------
  PASS: iOS backend C++20 syntax-only compile with local Dynarmic API stub.
  PASS: PT22 -> PT23 source patch reconstructs the PT23 tree exactly (verified during packaging).
