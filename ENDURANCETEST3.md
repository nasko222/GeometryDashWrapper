# EnduranceTest3

Focused regression recovery:

- restores the accepted Fix6 x86 frame loop and pacing behavior;
- removes the EnduranceTest1/2 recursive x86 scene-graph inspection;
- preserves the confirmed working 1.93 backup transport;
- keeps cursors visible in editors and hides them only during player gameplay;
- restores cursor hiding after Resume on legacy ARM and ARMv7;
- hides verified legacy ARM top-right pause controls;
- updates the 2.2 song guide every rendered editor frame;
- refreshes BPM/time markers and the editor grid around playtest transitions;
- retains the confirmed 2.2 sawblade/monster/death-X colour repair;
- keeps the freezing exact companion editor redirect disabled.

The song line, BPM guides and moving right-side black region remain runtime tests,
not confirmed fixes.
