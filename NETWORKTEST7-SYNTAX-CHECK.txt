NETWORKTEST7 C++20 SYNTAX CHECK
==============================

Command class:
  clang++ -std=c++20 -fsyntax-only -Wall -Wextra
  with the local Dynarmic interface syntax stub and project include paths.

Result: PASS (exit status 0)

Warnings:
- network_poll_log_count_: pre-existing unused field
- native_http_next_id_: Windows-only usage is excluded by this Linux syntax pass
- gl_array_buffer_binding_: pre-existing unused field
- gl_element_buffer_binding_: pre-existing unused field

Limit:
This validates the entire translation unit's non-Windows parse path. The
container lacks the Windows SDK headers and complete Windows/Zig toolchain, so
it cannot compile or link the _WIN32 WinHTTP path into an executable here.
